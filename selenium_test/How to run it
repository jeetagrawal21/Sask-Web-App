
#delete existing selenium docker container 
#run this in console 

docker build -t selenium/standalone-chrome . 

# then run 

docker compose up --build .  

#open the terminal for the container and go to app  

 cd app

#Then run the test file 

python3 test.py


You should be able to access selenium grid (http://localhost:4444/ui)

And see a running session (the code for viewing the session is "secret")

